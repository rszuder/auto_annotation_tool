# Zależności uruchomieniowe dla Auto-Annotation Tool
# Zainstaluj ręcznie poleceniem:
#   python -m pip install -r requirements-runtime.txt
#
# Tkinter nie jest tu wymieniony, ponieważ zazwyczaj jest dostarczany
# razem z Pythonem (instalator Windows / komponent Tcl-Tk), a nie przez pip.

numpy
opencv-python
Pillow
PyYAML
torch
ultralytics
easyocr
